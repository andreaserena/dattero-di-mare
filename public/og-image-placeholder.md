# TODO: OG Image

Sostituire questo file con `og-image.jpg` (1200×630 px).

Contenuto suggerito: foto del locale o di un piatto principale (tartare, plateau crudi).
Il file deve chiamarsi esattamente `og-image.jpg` nella root di `public/`.
